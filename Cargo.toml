[package]
name = "chronovm"
version = "0.1.0"
edition = "2021"
description = "A stack-based bytecode VM with a time-travel debugger — scrub execution backwards like a video."
license = "MIT"

[dependencies]
ratatui = "0.29"
crossterm = "0.28"

[profile.release]
opt-level = 3
lto = true
